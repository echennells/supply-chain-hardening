{
  "name": "test/postinstall-trigger",
  "description": "Fires post-install-cmd via a real (local) require. Tests whether composer script blocking is in effect.",
  "type": "project",
  "scripts": {
    "post-install-cmd": [
      "echo 'COMPOSER_SCRIPT_RAN' > /tmp/marker-composer-script"
    ]
  },
  "repositories": [
    { "type": "path", "url": "/opt/test-fixtures/composer-local-dep" }
  ],
  "require": {
    "test/composer-local-dep": "*"
  }
}
