; Inventory used by tests/matrix/run.sh when re-applying site.yml against
; the matrix host. The matrix is designed to run against a fresh Ubuntu
; host that already has the role's prereqs installed (ansible, git, bats,
; Sury PHP repo). The driver re-applies site.yml in-place between cells.

[local]
localhost ansible_connection=local
